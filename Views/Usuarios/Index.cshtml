@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
    ViewBag.Title = "Usuarios";
}

<div class="page-header">
    <div>
        <h2 class="section-title"><i class="fas fa-user-cog me-2"></i>Usuarios</h2>
        <p class="section-sub">Administración de usuarios del sistema.</p>
    </div>
</div>

<div class="card shadow-sm">
    <div class="card-header">
        <h5 class="mb-0"><i class="fas fa-users me-2"></i>Usuarios registrados</h5>
    </div>
    <div class="card-body text-center text-muted py-5">
        <i class="fas fa-tools fa-2x mb-3"></i>
        <p class="mb-0">Módulo en construcción. Aquí se listarán los usuarios del sistema.</p>
    </div>
</div>
